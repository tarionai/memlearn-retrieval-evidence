# Pinned environment that produced the LongMemEval-S n=200 results (Python 3.10.11, CPU-only).
# Direct dependencies of the benchmark harness chain plus the load-bearing transitives.
numpy==2.2.6
networkx==3.4.2
PyYAML==6.0.3
torch==2.11.0
transformers==5.5.4
tokenizers==0.22.2
safetensors==0.7.0
sentence-transformers==5.4.1
scipy==1.15.3
scikit-learn==1.7.2
bm25s==0.3.9
# Imported transitively by the harness's profile module (no DB connection is made in benchmark runs):
psycopg2-binary==2.9.12
# PPR arm only (not needed for the dense/OSAM/cross-encoder/hybrid smoke run):
spacy==3.8.14
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

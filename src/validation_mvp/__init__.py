"""Validation MVP — standalone external-memory benchmark harness (WP-110).

No Layer A-I imports; Primitive-First Gate N/A (apps/mem-learn is a standalone
validation tool). This package currently holds only the LongMemEval
retrieval-stage slice; the full 6-benchmark tree is deferred.
"""
